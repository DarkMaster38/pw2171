HTML5: ¿Que incluye?
- Video
- Audio
- Canvas (dibujar)
- Guardar datos en navegador
- Etiquetas semánticas
- etc.


Aside = Etiqueta que muestra información tangencial(No importa) de la página.
SEO


HTML = estructura + contenido
HTML + DISEÑO = CCS3
CCS = HOJAS DE ESTILO EN CASCADA
    = MAQUETADO = MAQUETACIÓN

Teoria CSS

    Orden en que van los elementos:
1.- Pseudo-elementos
    Contiene atributos
2.- Etiquetas <etiqueta>
    Contiene atributos
3.- Identificadores
    Contiene atributos
4.- Clases
    Contiene atributos
5.- Elemento de respnsive design
    (diseño adaptativo)
        Contiene atributos
Todos ellos en orden alfabético.
Tanto elementos como atributos.

-Comando

body{
	atributo:valor;
}